import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    minHeight: 48,
    maxHeight: 78,
  },
  tabsWrapper: {
    flexDirection: 'row',
    minHeight: 48,
    maxHeight: 78,
  },
});

export default styles;
