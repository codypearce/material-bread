import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    minHeight: 48,
    maxHeight: 72,
  },
  tabsWrapper: {
    flexDirection: 'row',
    minHeight: 48,
    maxHeight: 72,
  },
});

export default styles;
