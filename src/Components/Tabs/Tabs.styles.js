import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 48,
  },
  tabsWrapper: {
    flexDirection: 'row',
    height: 48,
  },
});

export default styles;
