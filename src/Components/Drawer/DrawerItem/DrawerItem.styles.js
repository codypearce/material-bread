import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  drawerItem: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 48,
    borderRadius: 6,
    paddingHorizontal: 8,
  },
  text: {
    fontSize: 14,
    color: 'rgba(0,0,0,.87)',
    fontWeight: '500',
  },
});

export default styles;
