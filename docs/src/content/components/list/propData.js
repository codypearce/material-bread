import { createTableData } from '../../../utils/createPropData';
const propData = [
  ['elevation', 'Shadow for list container', 'number', ''],
  ['style', 'Styles root element', 'object', ''],
];

export default createTableData(propData);
